#include <stdio.h>

int findMaxSum(int arr[], int n) {
    if (n == 0)
        return arr[0];
    int include = arr[n];
    int exclude = 0;
    return include;
}

int main() {
    int arr[] = {2, 3};
    int result = findMaxSum(arr, 0);
    return 0;
}
